# ScholarEase 文献上传与查询接口文档

## 1. 文档说明

本文档定义 ScholarEase 文献上传、解析入库和文献库查询的 MVP HTTP 接口。当前版本以本地文献库页面为主要前端入口，前端和后端上传入口都只处理 PDF；非 PDF 文件不进入上传逻辑。

当前 MVP 闭环：

```text
本地 PDF 上传
  -> 校验文件类型和大小
  -> 前端计算并提交 fileName、paperMd5、fileSizeBytes、submissionTime
  -> 后端复核 PDF 类型、文件大小和 MD5
  -> 按 paper_md5 在上传流程内查重
  -> 如文件 MD5 已存在，直接返回 409 DUPLICATE_PAPER，本次上传失败
  -> 如不重复，写入 paper_upload_parse_progress，parse_status = 1
  -> 调用 MinerU 解析 PDF
  -> 确认原始 PDF 写入 MinIO
  -> 解析完成后确认 MinerU 解析产物写入 MinIO、PDF 写入 Zotero
  -> 后续入库阶段再写入 MySQL papers 主表
  -> 更新 paper_upload_parse_progress.parse_status
  -> 文献库页面通过列表查询和详情查询展示结果
```

关键约束：

- 当前版本不做登录、注册、JWT、RBAC；接口暂不要求 `Authorization`。
- 上传接口只接收 PDF。非 PDF 文件返回 `INVALID_FILE_TYPE`。
- 上传接口除 PDF 文件本体外，前端只提交 `fileName`、`paperMd5`、`fileSizeBytes`、`submissionTime` 四个文件字段；标题、作者、关键词、年份、期刊、DOI 等学术元数据由后端解析或后续补全。
- 文献上传接口和重复文档检查能力合并：后端在上传工程中按 `paperMd5` 完成查重；只要判断为重复，就直接报错，不提供二次确认导入接口。
- MySQL 第一版以 `paper_upload_parse_progress` 承接上传与解析状态；当前实现先确认原始 PDF 写入 MinIO、Zotero 只保存 PDF、MinerU 解析产物写入 MinIO，`papers` 主表写入放到后续入库阶段，字段参考《MySQL库表设计1.md》。
- 文献查询接口只保留文献列表查询和文献详情查询。
- 前端文献库页面列表字段包括：文献文件名称、标题名称、作者姓名、存储位置、上传日期、操作。后端响应需要提供对应展示字段，其中文件名和存储位置第一版可由上传文件名和本地对象路径规则生成。

## 2. 基础约定

### 2.1 Base URL

```text
http://localhost:8080/api
```

### 2.2 时间格式

接口响应中的时间字段统一使用 ISO-8601 字符串：

```text
2026-05-12T10:30:00.123+08:00
```

数据库中的 `submission_time` 为 `DATETIME`，后端响应时转换为上述格式。

### 2.3 通用响应结构

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {},
  "traceId": "9e37d2f928b44f87",
  "timestamp": "2026-05-12T10:30:00.123+08:00"
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `code` | string | 是 | 业务状态码，成功固定为 `SUCCESS` |
| `message` | string | 是 | 面向前端展示或调试的简短信息 |
| `data` | object / array / null | 否 | 业务数据 |
| `traceId` | string | 是 | 请求追踪 ID |
| `timestamp` | string | 是 | 服务端响应时间 |

### 2.4 分页响应结构

```json
{
  "items": [],
  "page": 1,
  "pageSize": 20,
  "total": 128,
  "hasNext": true
}
```

分页参数统一从 1 开始：

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---:|---|
| `page` | integer | 1 | 页码，从 1 开始 |
| `pageSize` | integer | 20 | 每页数量，建议最大 100 |

## 3. 状态枚举

### 3.1 论文解析状态 `parseStatus`

接口层使用字符串状态；数据库 `paper_upload_parse_progress.parse_status` 使用整数存储。

| 接口值 | 数据库值 | 说明 |
|---|---:|---|
| `PENDING` | 0 | 等待解析 |
| `PARSING` | 1 | 解析中 |
| `PARSED` | 2 | 解析完成 |
| `FAILED` | 3 | 解析失败 |

## 4. 数据模型

### 4.1 Paper 数据库字段映射

当前第一版规划以 `papers` 主表作为文献列表和详情的数据来源。当前已实现链路先只确认 PDF 写入 Zotero、MinerU 解析产物写入 MinIO；`papers` 主表保存解析完成后的最终文献数据属于后续入库阶段：

| 接口字段 | 数据库字段 | 说明 |
|---|---|---|
| `paperId` | `id` | 论文 ID |
| `paperMd5` | `paper_md5` | 文件 MD5，作为文件唯一标识和主要查重依据 |
| `fileName` | `file_name` | 上传文件原始名称 |
| `fileSizeBytes` | `file_size_bytes` | 文件大小，单位字节 |
| `title` | `title` | 论文标题 |
| `authors` | `authors_json` | 作者数组，由 JSON 转为 string[] |
| `authorText` | `authors_json` | 前端列表展示用作者字符串 |
| `keywords` | `keywords_json` | 关键词数组，由 JSON 转为 string[] |
| `language` | `language` | 论文语言 |
| `year` | `year` | 发表年份 |
| `venue` | `venue` | 期刊 / 会议 / 出版源 |
| `doi` | `doi` | DOI |
| `uploadTime` | `submission_time` | 上传 / 提交时间 |

前端页面需要但 `papers` 表暂未显式保存的字段：

| 接口字段 | 第一版来源 | 说明 |
|---|---|---|
| `fileType` | 上传文件扩展名 | 第一版固定为 `PDF` |
| `storageLocation` | 后端对象路径规则 | 存储位置，例如 `/docs/papers/2026/` 或 `papers/{paperId}/original/` |

后续如需要严格保留对象位置，建议在 `papers` 表新增 `storage_location` 字段，或拆分 `paper_files` / `paper_attachments` 表；本接口字段名保持不变。

### 4.2 PaperSummary

用于文献库列表页。

```json
{
  "paperId": 10001,
  "paperMd5": "1f3870be274f6c49b3e31a0c6728957f",
  "fileName": "attention.pdf",
  "fileSizeBytes": 2148123,
  "fileType": "PDF",
  "title": "Attention Is All You Need",
  "authors": ["Ashish Vaswani", "Noam Shazeer"],
  "authorText": "Ashish Vaswani, Noam Shazeer",
  "storageLocation": "papers/10001/original/",
  "uploadTime": "2026-05-12T10:30:00.123+08:00",
  "year": 2017,
  "venue": "NeurIPS",
  "doi": "10.5555/3295222.3295349"
}
```

字段说明：

| 字段 | 类型 | 说明 |
|---|---|---|
| `paperId` | long | 论文 ID |
| `paperMd5` | string | 文件 MD5 |
| `fileName` | string | 文献文件名称，对应前端表格“文献文件名称” |
| `fileSizeBytes` | long | 文件大小，单位字节 |
| `fileType` | string | 文件类型，第一版固定为 `PDF` |
| `title` | string | 标题名称，对应前端表格“标题名称” |
| `authors` | string[] | 作者数组 |
| `authorText` | string | 作者姓名展示文本，对应前端表格“作者姓名” |
| `storageLocation` | string | 存储位置，对应前端表格“存储位置” |
| `uploadTime` | string | 上传日期 / 时间，对应前端表格“上传日期” |
| `year` | integer | 发表年份 |
| `venue` | string | 期刊 / 会议 / 出版源 |
| `doi` | string | DOI |

### 4.3 PaperDetail

用于文献详情页。详情模型在 `PaperSummary` 基础上补充关键词、语言和可用于展示 / 调试的解析信息。

```json
{
  "paperId": 10001,
  "paperMd5": "1f3870be274f6c49b3e31a0c6728957f",
  "fileName": "attention.pdf",
  "fileSizeBytes": 2148123,
  "fileType": "PDF",
  "title": "Attention Is All You Need",
  "authors": ["Ashish Vaswani", "Noam Shazeer"],
  "authorText": "Ashish Vaswani, Noam Shazeer",
  "keywords": ["transformer", "attention"],
  "language": "en",
  "year": 2017,
  "venue": "NeurIPS",
  "doi": "10.5555/3295222.3295349",
  "storageLocation": "papers/10001/original/",
  "uploadTime": "2026-05-12T10:30:00.123+08:00"
}
```

字段说明：

| 字段 | 类型 | 说明 |
|---|---|---|
| `keywords` | string[] | 关键词数组，由 `keywords_json` 转换 |
| `language` | string | 论文语言 |
| 其他字段 | - | 同 `PaperSummary` |

## 5. 文献上传接口

### 5.1 上传 PDF

```http
POST /api/documents/upload
Content-Type: multipart/form-data
```

用于上传本地 PDF，并在同一流程内完成重复文献检查。上传接口不返回待确认的中间态；只要命中重复规则，本次上传直接失败。

表单字段：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `traceId` | string | 是 | 前端为本次上传生成的追踪 ID，必须放在表单字段最前面；用于写入上传解析进度表 |
| `file` | file | 是 | PDF 文件，MIME 建议为 `application/pdf` |
| `fileName` | string | 是 | 文件原始名称，例如 `attention.pdf` |
| `paperMd5` | string | 是 | 前端根据文件内容计算出的 32 位 MD5 |
| `fileSizeBytes` | long | 是 | 前端根据 `File.size` 计算出的文件大小，单位字节 |
| `submissionTime` | string | 是 | 前端发起上传时生成的提交时间，ISO-8601 字符串 |

字段顺序约定：

- 前端当前实现会先生成 `traceId`，然后按 `traceId`、`file`、`fileName`、`paperMd5`、`fileSizeBytes`、`submissionTime` 的顺序追加到 `FormData`。
- `traceId` 表示“本次上传请求”；`paperMd5` 表示“文件内容”。同一个 PDF 重新上传时，`paperMd5` 可能相同，但 `traceId` 应重新生成。
- 响应体中的 `traceId` 与请求表单中的上传 `traceId` 保持一致，便于前端、上传进度表和后端日志按同一条链路排查。

请求示例：

```bash
curl -X POST "http://localhost:8080/api/documents/upload" \
  -F "traceId=9e37d2f928b44f87" \
  -F "file=@attention.pdf" \
  -F "fileName=attention.pdf" \
  -F "paperMd5=1f3870be274f6c49b3e31a0c6728957f" \
  -F "fileSizeBytes=2148123" \
  -F "submissionTime=2026-05-12T10:30:00.123+08:00"
```

成功响应：

```json
{
  "code": "SUCCESS",
  "message": "Upload succeeded; parsing started",
  "data": {
    "traceId": "9e37d2f928b44f87",
    "paperMd5": "1f3870be274f6c49b3e31a0c6728957f",
    "fileName": "attention.pdf",
    "fileSizeBytes": 2148123,
    "submissionTime": "2026-05-12T10:30:00.123+08:00",
    "parseStatus": "PARSING"
  },
  "traceId": "9e37d2f928b44f87",
  "timestamp": "2026-05-12T10:30:00.123+08:00"
}
```

业务规则：

- 文件扩展名必须为 `.pdf`，且 MIME 类型应为 `application/pdf`。
- 建议单文件大小上限为 200 MB；超过限制返回 `FILE_TOO_LARGE`。
- 前端必须提交 `traceId`、`fileName`、`paperMd5`、`fileSizeBytes`、`submissionTime`。
- 后端必须重新计算文件 MD5 和文件大小，并与前端提交的 `paperMd5`、`fileSizeBytes` 对比；不一致时返回 `FILE_METADATA_MISMATCH`。
- 后端接收请求后，应将 `traceId`、`paperMd5`、`fileName`、`submissionTime` 写入 `paper_upload_parse_progress` 表：
  - `trace_id`：来自前端 `traceId`，作为本次上传请求的主键。
  - `paper_md5`：来自前端 `paperMd5`。
  - `filename`：来自前端 `fileName`。
  - `submission_time`：来自前端 `submissionTime`，后端解析后存为 MySQL `DATETIME`。
  - `parse_status`：上传成功后初始化为 `1`，即 `PARSING`。
- 当前后端代码中，`UploadDocumentDTO.traceId` 负责接收该字段；`PaperUploadParseProgressService.recordUploadProgress(...)` 负责组装 `PaperUploadParseProgressEntity` 并通过 `PaperUploadParseProgressMapper` 写入进度表。
- 上传阶段查重规则：`paper_md5` 完全一致时认为是重复文件，返回 `DUPLICATE_PAPER`。
- DOI、标题、第一作者、年份等学术元数据不由前端上传，后续如需要基于这些字段做更复杂的重复检查，应在解析 / 元数据补全阶段单独实现。
- 命中重复规则时，后端不写入新的 `papers` 记录，不写入新的 `paper_upload_parse_progress` 解析中记录，不启动解析。
- 未命中重复规则时，上传阶段只写入 `paper_upload_parse_progress`；当前实现先完成原始 PDF 上传 MinIO、MinerU 解析 ZIP 下载、解析产物上传 MinIO、PDF 写入 Zotero，`papers` 主表写入放到后续入库阶段：
  - `paper_md5`：文件 MD5。
  - `file_name`：上传文件原始名称。
  - `file_size_bytes`：文件大小，单位字节。
  - `title`、`authors_json`、`keywords_json`、`language`、`year`、`venue`、`doi`：来自解析结果或元数据补全结果。
  - `submission_time`：沿用上传请求的 `submissionTime`。
- 原始 PDF 上传 MinIO、MinerU 解析成功、解析产物上传 MinIO 且 PDF 写入 Zotero 后，更新 `paper_upload_parse_progress.parse_status = 2`；解析失败时更新为 `3`。

常见错误：

| HTTP 状态 | code | 说明 |
|---:|---|---|
| 400 | `INVALID_FILE_TYPE` | 文件不是 PDF |
| 400 | `FILE_METADATA_MISMATCH` | 前端提交的 MD5 或文件大小与后端复核结果不一致 |
| 413 | `FILE_TOO_LARGE` | 文件超过大小限制 |
| 409 | `DUPLICATE_PAPER` | 重复文献，上传被拒绝 |
| 502 | `MINERU_PARSE_FAILED` | MinerU 解析失败，文献主表记录未创建，上传进度状态为失败 |
| 500 | `UPLOAD_FAILED` | 上传流程内部失败 |

重复错误响应示例：

```json
{
  "code": "DUPLICATE_PAPER",
  "message": "检测到重复文献，上传已取消",
  "data": {
    "duplicateReason": "PDF_MD5_MATCHED",
    "existingPaper": {
      "paperId": 10001,
      "title": "Attention Is All You Need",
      "authors": ["Ashish Vaswani", "Noam Shazeer"],
      "paperMd5": "1f3870be274f6c49b3e31a0c6728957f",
      "fileName": "attention.pdf",
      "fileSizeBytes": 2148123,
      "uploadTime": "2026-05-12T10:30:00.123+08:00"
    }
  },
  "traceId": "9e37d2f928b44f87",
  "timestamp": "2026-05-12T10:31:00.123+08:00"
}
```

`duplicateReason` 可选值：

| 值 | 说明 |
|---|---|
| `PDF_MD5_MATCHED` | 文件 MD5 完全一致 |

### 5.2 MinerU 解析接口参考

上传接口校验通过且未命中重复文献后，后端通过 MinerU signed upload 流程解析 PDF。该流程属于后端内部调用，不要求前端额外提供本地文件路径、浏览器 `blob:` URL 或公网可访问文件地址。

推荐流程：

```text
后端接收前端 MultipartFile
  -> 校验 PDF / MD5 / 文件大小
  -> 写入 paper_upload_parse_progress，parse_status = 1
  -> 调用 MinerU 申请 signed upload URL
  -> 将原始 PDF 上传到 MinIO
  -> 后端 PUT 上传 PDF 文件流到 MinerU signed URL
  -> MinerU 自动开始解析
  -> 后端轮询 batch 解析结果
  -> 成功后获取 full_zip_url
  -> 下载解析 ZIP，提取 full.md、content_list_v2.json
  -> 将解析产物上传到 MinIO
  -> 将 PDF 写入 Zotero
  -> 后续入库阶段再写入 papers 主表
  -> 更新 paper_upload_parse_progress.parse_status = 2
```

#### 5.2.1 配置建议

```yaml
mineru:
  enabled: true
  baseUrl: https://mineru.net
  token: ${MINERU_TOKEN:}
  modelVersion: vlm
  sourceFile:
    mode: mineru-signed-upload
  api:
    fileUrlsBatchPath: /api/v4/file-urls/batch
    batchResultsPath: /api/v4/extract-results/batch/{batchId}
  polling:
    interval: 5s
    maxAttempts: 60
minio:
  endpoint: http://localhost:9000
  accessKey: minioadmin
  secretKey: minioadmin
  bucketName: literatures
  storage:
    originalPrefix: uploads/{traceId}/original/
    parsedPrefix: uploads/{traceId}/mineru/
```

`MINERU_TOKEN` 必须通过环境变量或部署平台密钥配置提供，不应明文写入 Git。

#### 5.2.2 申请 signed upload URL

```http
POST https://mineru.net/api/v4/file-urls/batch
Content-Type: application/json
Authorization: Bearer ${MINERU_TOKEN}
```

请求体：

```json
{
  "files": [
    {
      "name": "attention.pdf",
      "data_id": "9e37d2f928b44f87"
    }
  ],
  "model_version": "vlm"
}
```

字段约定：

| 字段 | 来源 | 说明 |
|---|---|---|
| `files[0].name` | 上传接口 `fileName` | PDF 文件名 |
| `files[0].data_id` | 上传接口 `traceId` | 用于关联本次上传链路和 MinerU 解析结果 |
| `model_version` | `mineru.modelVersion` | 第一版建议使用 `vlm` |

成功响应核心字段：

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "batch_id": "83add1c8-4845-4731-859b-9f47fccd971f",
    "file_urls": [
      "https://..."
    ]
  }
}
```

后端需要保存或至少在当前流程中保留 `batch_id` 和 `file_urls[0]`。如申请失败，应更新 `paper_upload_parse_progress.parse_status = 3`，并返回或记录 `MINERU_PARSE_FAILED`。

#### 5.2.3 上传 PDF 到 signed URL

使用 MinerU 返回的 `file_urls[0]` 执行 HTTP PUT，直接上传 PDF 文件字节：

```http
PUT {file_urls[0]}
```

实现约束：

- Java 后端应直接 PUT `MultipartFile` 的输入流或临时文件内容。
- 上传 signed URL 时不要依赖前端提供本地文件路径。
- 上传时不要额外强行设置不匹配的 `Content-Type`。
- PUT 成功的 HTTP 状态码应为 `200`；失败时更新 `paper_upload_parse_progress.parse_status = 3`。

#### 5.2.4 轮询解析结果

```http
GET https://mineru.net/api/v4/extract-results/batch/{batchId}
Authorization: Bearer ${MINERU_TOKEN}
```

成功响应核心结构：

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "batch_id": "83add1c8-4845-4731-859b-9f47fccd971f",
    "extract_result": [
      {
        "data_id": "9e37d2f928b44f87",
        "file_name": "attention.pdf",
        "state": "done",
        "err_msg": "",
        "full_zip_url": "https://..."
      }
    ]
  }
}
```

轮询规则建议：

| 配置 | 建议值 | 说明 |
|---|---|---|
| `polling.interval` | `5s` | 每 5 秒查询一次 |
| `polling.maxAttempts` | `60` | 最长等待约 5 分钟 |

状态处理：

| MinerU `state` | 后端处理 |
|---|---|
| `running` | 继续轮询 |
| `done` | 读取 `full_zip_url`，下载解析 ZIP，提取 `full.md`、`content_list_v2.json` 并上传 MinIO；不上传 images |
| `failed` | 读取 `err_msg`，更新解析状态为失败 |

当轮询超时、`state=failed`、`state=done` 但 `full_zip_url` 为空，或下载解析 ZIP 失败时，统一视为 MinerU 解析失败：更新 `paper_upload_parse_progress.parse_status = 3`，不写入 `papers` 主表。

#### 5.2.5 后端组件建议

| 组件 | 职责 |
|---|---|
| `MinerUProperties` | 绑定 `application.yml` 中的 `mineru` 配置 |
| `MinerUClient` | 封装 HTTP 调用：申请 URL、PUT 文件、轮询结果、下载 ZIP |
| `ObjectStorageService` | 封装 MinIO 上传；当前原始 PDF object key 使用 `uploads/{traceId}/original/{fileName}`，解析产物 object key 使用 `uploads/{traceId}/mineru/...` |
| `MinerUParseService` | 面向业务层提供 `parse(MultipartFile file, String traceId, String fileName)` |
| `MinerUParseResult` | 承载 `batchId`、`state`、`fullZipUrl` 等结果；解析产物正文不再传给 Zotero |

`DocumentImportService` 或等价上传业务服务只依赖 `MinerUParseService`，不直接拼接 MinerU HTTP 请求。Zotero 只保存 PDF 主文件；MinerU 返回的 Markdown / JSON 写入 MinIO，images 暂不上传，可作为后续提取 `title`、`authors_json`、`keywords_json`、`language`、`year`、`venue`、`doi` 的输入。

## 6. 文献查询接口

### 6.1 查询文献列表

```http
GET /api/documents
```

用于文献库页面表格展示、搜索、筛选和分页。

查询参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `keyword` | string | 否 | 搜索文件名、标题、作者、DOI、期刊 / 会议 |
| `year` | integer | 否 | 发表年份 |
| `venue` | string | 否 | 期刊 / 会议 / 出版源 |
| `page` | integer | 否 | 页码，默认 1 |
| `pageSize` | integer | 否 | 每页数量，默认 20 |

成功响应：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {
    "items": [
      {
        "paperId": 10001,
        "paperMd5": "1f3870be274f6c49b3e31a0c6728957f",
        "fileName": "attention.pdf",
        "fileSizeBytes": 2148123,
        "fileType": "PDF",
        "title": "Attention Is All You Need",
        "authors": ["Ashish Vaswani", "Noam Shazeer"],
        "authorText": "Ashish Vaswani, Noam Shazeer",
        "storageLocation": "papers/10001/original/",
        "uploadTime": "2026-05-12T10:30:00.123+08:00",
        "year": 2017,
        "venue": "NeurIPS",
        "doi": "10.5555/3295222.3295349"
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 1,
    "hasNext": false
  },
  "traceId": "9e37d2f928b44f87",
  "timestamp": "2026-05-12T10:40:00.123+08:00"
}
```

列表排序建议：

- 默认按 `submission_time DESC` 排序。
- `submission_time` 为空的历史数据排在最后。

前端字段对应：

| 前端列 | 接口字段 |
|---|---|
| 文献文件名称 | `fileName`、`fileType` |
| 标题名称 | `title` |
| 作者姓名 | `authorText` |
| 存储位置 | `storageLocation` |
| 上传日期 | `uploadTime` |
| 操作 | 前端根据 `paperId` 发起详情查看等动作 |

### 6.2 查询文献详情

```http
GET /api/documents/{paperId}
```

用于进入文献详情页或列表操作中的预览 / 查看详情。

路径参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `paperId` | long | 是 | 论文 ID |

成功响应：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {
    "paperId": 10001,
    "paperMd5": "1f3870be274f6c49b3e31a0c6728957f",
    "fileName": "attention.pdf",
    "fileSizeBytes": 2148123,
    "fileType": "PDF",
    "title": "Attention Is All You Need",
    "authors": ["Ashish Vaswani", "Noam Shazeer"],
    "authorText": "Ashish Vaswani, Noam Shazeer",
    "keywords": ["transformer", "attention"],
    "language": "en",
    "year": 2017,
    "venue": "NeurIPS",
    "doi": "10.5555/3295222.3295349",
    "storageLocation": "papers/10001/original/",
    "uploadTime": "2026-05-12T10:30:00.123+08:00"
  },
  "traceId": "9e37d2f928b44f87",
  "timestamp": "2026-05-12T10:40:00.123+08:00"
}
```

错误：

| HTTP 状态 | code | 说明 |
|---:|---|---|
| 404 | `PAPER_NOT_FOUND` | 论文不存在 |

## 7. 错误码

| code | HTTP 状态 | 说明 |
|---|---:|---|
| `SUCCESS` | 200 | 成功 |
| `BAD_REQUEST` | 400 | 请求参数错误 |
| `INVALID_FILE_TYPE` | 400 | 不支持的文件类型 |
| `FILE_METADATA_MISMATCH` | 400 | 前端提交的文件 MD5 或大小与后端复核结果不一致 |
| `FILE_TOO_LARGE` | 413 | 文件过大 |
| `PAPER_NOT_FOUND` | 404 | 论文不存在 |
| `DUPLICATE_PAPER` | 409 | 重复文献 |
| `MINERU_PARSE_FAILED` | 502 | MinerU API 解析失败 |
| `UPLOAD_FAILED` | 500 | 上传流程内部失败 |
| `INTERNAL_ERROR` | 500 | 服务端未知错误 |

错误响应示例：

```json
{
  "code": "FILE_METADATA_MISMATCH",
  "message": "文件 MD5 或大小校验失败",
  "data": {
    "expectedPaperMd5": "1f3870be274f6c49b3e31a0c6728957f",
    "actualPaperMd5": "0cc175b9c0f1b6a831c399e269772661",
    "expectedFileSizeBytes": 2148123,
    "actualFileSizeBytes": 2148120
  },
  "traceId": "9e37d2f928b44f87",
  "timestamp": "2026-05-12T10:30:00.123+08:00"
}
```

## 8. 核心业务流程

### 8.1 本地上传成功流程

```text
POST /api/documents/upload
  -> 校验 PDF 类型和大小
  -> 接收 fileName / paperMd5 / fileSizeBytes / submissionTime
  -> 后端复核 MD5 和文件大小
  -> 按 paper_md5 查重
  -> 未命中重复
  -> 写入 paper_upload_parse_progress:
       trace_id
       paper_md5
       filename
       submission_time
       parse_status = 1
  -> 上传原始 PDF 到 MinIO
  -> 调用 MinerU 解析
  -> 解析成功后下载 MinerU ZIP
  -> 解压 full.md、content_list_v2.json 并上传 MinIO
  -> 确认 PDF 写入 Zotero
  -> 后续入库阶段写入 papers:
       paper_md5
       file_name
       file_size_bytes
       title
       authors_json
       keywords_json
       language
       year
       venue
       doi
       submission_time
  -> 更新 paper_upload_parse_progress.parse_status = 2
  -> 返回上传已提交解析
```

### 8.2 重复文献拦截流程

```text
POST /api/documents/upload
  -> 校验 PDF 类型和大小
  -> 接收 fileName / paperMd5 / fileSizeBytes / submissionTime
  -> 后端复核 MD5 和文件大小
  -> 按 paper_md5 查重
  -> 命中重复
  -> 返回 409 DUPLICATE_PAPER
  -> 不写入新的 papers 记录
  -> 不写入新的 paper_upload_parse_progress 解析中记录
  -> 不启动 MinerU 解析
```

### 8.3 MinerU 解析失败流程

```text
POST /api/documents/upload
  -> 已写入 paper_upload_parse_progress，parse_status = 1
  -> MinerU API failed
  -> 更新 paper_upload_parse_progress.parse_status = 3
  -> 不写入 papers 主表
  -> 返回 MINERU_PARSE_FAILED 或返回上传成功但进度状态为 FAILED
```

第一版建议：如果解析失败，此时不创建 `papers` 主表记录；错误响应可在 `data.traceId` 中带上本次上传追踪 ID，便于前端查询 `paper_upload_parse_progress` 或提示用户。

## 9. 前端接入建议

### 9.1 文献库页面

推荐接口：

- `GET /api/documents`：加载表格。
- `POST /api/documents/upload`：上传 PDF。
- `GET /api/documents/{paperId}`：进入详情页。

上传交互建议：

- 前端只处理 PDF 文件，不处理 DOC / DOCX。
- 前端提交上传时，除 PDF 文件本体外，只传 `fileName`、`paperMd5`、`fileSizeBytes`、`submissionTime`。
- 前端可先做文件类型、大小和 MD5 计算，但后端必须重复校验。
- 上传成功后刷新 `GET /api/documents`。
- 收到 `DUPLICATE_PAPER` 时直接提示上传失败，并展示 `data.existingPaper` 中的已存在文献信息。
- 收到 `MINERU_PARSE_FAILED` 时提示解析失败；如果响应中存在 `data.paperId`，可允许用户在列表中看到失败状态。

### 9.2 文献列表字段

前端 `LibraryView.vue` 当前表格列与接口字段的建议绑定关系：

| 页面列 | 建议绑定 |
|---|---|
| 文献文件名称 | `fileType` 徽标 + `fileName` |
| 标题名称 | `title` |
| 作者姓名 | `authorText` |
| 存储位置 | `storageLocation` |
| 上传日期 | `uploadTime` 格式化为 `YYYY-MM-DD` |
| 操作 | `paperId` |

## 10. 后端实现建议

### 10.1 Controller 划分

```text
DocumentController
  POST /api/documents/upload
  GET  /api/documents
  GET  /api/documents/{paperId}
```

### 10.2 Service 调用边界

```text
DocumentController
  -> DocumentImportService
  -> PaperService

DocumentImportService
  -> DuplicatePaperService
  -> MinerUParseService
  -> PaperService
```

### 10.3 幂等性和查重建议

- `paper_md5` 是上传接口的主要幂等依据，数据库已设置唯一约束。
- `file_name` 和 `file_size_bytes` 由前端提交，后端复核后写入主表。
- 后端需要捕获 `paper_md5` 唯一键冲突，并统一转换为 `DUPLICATE_PAPER`。
- DOI / 标题相似查重不属于上传接口的前端入参范围；如后续要做，应基于解析后的元数据异步补充，避免阻塞当前上传链路。

## 11. OpenAPI 片段示例

```yaml
paths:
  /api/documents/upload:
    post:
      summary: 上传本地 PDF，重复文献直接报错
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - fileName
                - paperMd5
                - fileSizeBytes
                - submissionTime
              properties:
                file:
                  type: string
                  format: binary
                fileName:
                  type: string
                paperMd5:
                  type: string
                fileSizeBytes:
                  type: integer
                  format: int64
                submissionTime:
                  type: string
                  format: date-time
      responses:
        "200":
          description: 上传成功
        "400":
          description: 参数或文件类型错误
        "409":
          description: 重复文献
        "413":
          description: 文件过大
        "502":
          description: MinerU 解析失败
  /api/documents:
    get:
      summary: 查询文献列表
  /api/documents/{paperId}:
    get:
      summary: 查询文献详情
```

## 12. 第一版落地优先级

建议按以下顺序实现接口：

1. `POST /api/documents/upload`
2. `GET /api/documents`
3. `GET /api/documents/{paperId}`

这样可以先跑通“上传 PDF -> 查重 -> 写入 papers -> MinerU 解析 -> 列表 / 详情展示”的最小闭环。
