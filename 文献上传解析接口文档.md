# ScholarEase 文献上传与查询接口文档

## 1. 文档说明

本文档定义 ScholarEase 文献上传、解析入库和文献库查询的 MVP HTTP 接口。当前版本以本地文献库页面为主要前端入口，后端上传入口只支持 PDF；前端可以保留 DOC / DOCX 本地预览能力，但真正提交到后端时必须由后端再次校验并拒绝非 PDF 文件。

当前 MVP 闭环：

```text
本地 PDF 上传
  -> 校验文件类型和大小
  -> 计算 paper_md5
  -> 在上传流程内查重
  -> 如疑似重复，直接返回 409 DUPLICATE_PAPER，本次上传失败
  -> 如不重复，写入 MySQL papers 主表，parse_status = 1
  -> 调用 MinerU 解析 PDF
  -> 根据解析结果更新 papers 元数据和 parse_status
  -> 文献库页面通过列表查询和详情查询展示结果
```

关键约束：

- 当前版本不做登录、注册、JWT、RBAC；接口暂不要求 `Authorization`。
- 上传接口只接收 PDF。非 PDF 文件返回 `INVALID_FILE_TYPE`。
- 文献上传接口和“是否疑似重复文档”能力合并：后端在上传工程中完成查重；只要判断为重复或疑似重复，就直接报错，不提供二次确认导入接口。
- MySQL 第一版以 `papers` 主表为准，字段参考《MySQL库表设计1.md》。
- 文献查询接口只保留文献列表查询和文献详情查询。
- 前端文献库页面列表字段包括：文献文件名称、标题名称、作者姓名、存储位置、上传日期、操作。后端响应需要提供对应展示字段，其中文件名和存储位置第一版可由上传文件名和本地对象路径规则生成。

## 2. 基础约定

### 2.1 Base URL

```text
http://localhost:8080/api
```

### 2.2 时间格式

接口响应中的时间字段统一使用 ISO-8601 字符串：

```text
2026-05-12T10:30:00.123+08:00
```

数据库中的 `submission_time` 为 `DATETIME(3)`，后端响应时转换为上述格式。

### 2.3 通用响应结构

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {},
  "traceId": "9e37d2f928b44f87",
  "timestamp": "2026-05-12T10:30:00.123+08:00"
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `code` | string | 是 | 业务状态码，成功固定为 `SUCCESS` |
| `message` | string | 是 | 面向前端展示或调试的简短信息 |
| `data` | object / array / null | 否 | 业务数据 |
| `traceId` | string | 是 | 请求追踪 ID |
| `timestamp` | string | 是 | 服务端响应时间 |

### 2.4 分页响应结构

```json
{
  "items": [],
  "page": 1,
  "pageSize": 20,
  "total": 128,
  "hasNext": true
}
```

分页参数统一从 1 开始：

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---:|---|
| `page` | integer | 1 | 页码，从 1 开始 |
| `pageSize` | integer | 20 | 每页数量，建议最大 100 |

## 3. 状态枚举

### 3.1 论文解析状态 `parseStatus`

接口层使用字符串状态；数据库 `papers.parse_status` 使用整数存储。

| 接口值 | 数据库值 | 说明 |
|---|---:|---|
| `PENDING` | 0 | 等待解析 |
| `PARSING` | 1 | 解析中 |
| `PARSED` | 2 | 解析完成 |
| `FAILED` | 3 | 解析失败 |

## 4. 数据模型

### 4.1 Paper 数据库字段映射

当前第一版以 `papers` 主表作为接口数据来源：

| 接口字段 | 数据库字段 | 说明 |
|---|---|---|
| `paperId` | `id` | 论文 ID |
| `paperMd5` | `paper_md5` | 文件 MD5，作为文件唯一标识和主要查重依据 |
| `title` | `title` | 论文标题 |
| `authors` | `authors_json` | 作者数组，由 JSON 转为 string[] |
| `authorText` | `authors_json` | 前端列表展示用作者字符串 |
| `keywords` | `keywords_json` | 关键词数组，由 JSON 转为 string[] |
| `language` | `language` | 论文语言 |
| `year` | `year` | 发表年份 |
| `venue` | `venue` | 期刊 / 会议 / 出版源 |
| `doi` | `doi` | DOI |
| `uploadTime` | `submission_time` | 上传 / 提交时间 |
| `parseStatus` | `parse_status` | 解析状态，按枚举映射 |

前端页面需要但 `papers` 表暂未显式保存的字段：

| 接口字段 | 第一版来源 | 说明 |
|---|---|---|
| `fileName` | 上传文件原始名称；若未落库，可由标题或 `paperMd5 + ".pdf"` 兜底 | 文献文件名称 |
| `fileType` | 上传文件扩展名 | 第一版固定为 `PDF` |
| `storageLocation` | 后端对象路径规则 | 存储位置，例如 `/docs/papers/2026/` 或 `papers/{paperId}/original/` |

后续如需要严格保留文件名和对象位置，建议在 `papers` 表新增 `file_name`、`storage_location` 字段，或拆分 `paper_files` / `paper_attachments` 表；本接口字段名保持不变。

### 4.2 PaperSummary

用于文献库列表页。

```json
{
  "paperId": 10001,
  "paperMd5": "1f3870be274f6c49b3e31a0c6728957f",
  "fileName": "attention.pdf",
  "fileType": "PDF",
  "title": "Attention Is All You Need",
  "authors": ["Ashish Vaswani", "Noam Shazeer"],
  "authorText": "Ashish Vaswani, Noam Shazeer",
  "storageLocation": "papers/10001/original/",
  "uploadTime": "2026-05-12T10:30:00.123+08:00",
  "year": 2017,
  "venue": "NeurIPS",
  "doi": "10.5555/3295222.3295349",
  "parseStatus": "PARSED"
}
```

字段说明：

| 字段 | 类型 | 说明 |
|---|---|---|
| `paperId` | long | 论文 ID |
| `paperMd5` | string | 文件 MD5 |
| `fileName` | string | 文献文件名称，对应前端表格“文献文件名称” |
| `fileType` | string | 文件类型，第一版固定为 `PDF` |
| `title` | string | 标题名称，对应前端表格“标题名称” |
| `authors` | string[] | 作者数组 |
| `authorText` | string | 作者姓名展示文本，对应前端表格“作者姓名” |
| `storageLocation` | string | 存储位置，对应前端表格“存储位置” |
| `uploadTime` | string | 上传日期 / 时间，对应前端表格“上传日期” |
| `year` | integer | 发表年份 |
| `venue` | string | 期刊 / 会议 / 出版源 |
| `doi` | string | DOI |
| `parseStatus` | string | 解析状态 |

### 4.3 PaperDetail

用于文献详情页。详情模型在 `PaperSummary` 基础上补充关键词、语言和可用于展示 / 调试的解析信息。

```json
{
  "paperId": 10001,
  "paperMd5": "1f3870be274f6c49b3e31a0c6728957f",
  "fileName": "attention.pdf",
  "fileType": "PDF",
  "title": "Attention Is All You Need",
  "authors": ["Ashish Vaswani", "Noam Shazeer"],
  "authorText": "Ashish Vaswani, Noam Shazeer",
  "keywords": ["transformer", "attention"],
  "language": "en",
  "year": 2017,
  "venue": "NeurIPS",
  "doi": "10.5555/3295222.3295349",
  "storageLocation": "papers/10001/original/",
  "uploadTime": "2026-05-12T10:30:00.123+08:00",
  "parseStatus": "PARSED"
}
```

字段说明：

| 字段 | 类型 | 说明 |
|---|---|---|
| `keywords` | string[] | 关键词数组，由 `keywords_json` 转换 |
| `language` | string | 论文语言 |
| 其他字段 | - | 同 `PaperSummary` |

## 5. 文献上传接口

### 5.1 上传 PDF

```http
POST /api/documents/upload
Content-Type: multipart/form-data
```

用于上传本地 PDF，并在同一流程内完成重复文献检查。上传接口不再返回“疑似重复待确认”的中间态；只要命中重复规则，本次上传直接失败。

表单字段：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `file` | file | 是 | PDF 文件，MIME 建议为 `application/pdf` |
| `title` | string | 否 | 论文标题；为空时可从文件名或 MinerU 解析结果推断 |
| `authorsJson` | string | 否 | 作者数组 JSON 字符串，如 `["A","B"]` |
| `keywordsJson` | string | 否 | 关键词数组 JSON 字符串 |
| `language` | string | 否 | 论文语言，默认 `en` |
| `year` | integer | 否 | 发表年份 |
| `venue` | string | 否 | 期刊 / 会议 / 出版源 |
| `doi` | string | 否 | DOI |

请求示例：

```bash
curl -X POST "http://localhost:8080/api/documents/upload" \
  -F "file=@attention.pdf" \
  -F "title=Attention Is All You Need" \
  -F "authorsJson=[\"Ashish Vaswani\",\"Noam Shazeer\"]" \
  -F "keywordsJson=[\"transformer\",\"attention\"]" \
  -F "language=en" \
  -F "year=2017" \
  -F "venue=NeurIPS" \
  -F "doi=10.5555/3295222.3295349"
```

成功响应：

```json
{
  "code": "SUCCESS",
  "message": "上传成功，文献解析已开始",
  "data": {
    "paperId": 10001,
    "paperMd5": "1f3870be274f6c49b3e31a0c6728957f",
    "fileName": "attention.pdf",
    "fileType": "PDF",
    "title": "Attention Is All You Need",
    "authors": ["Ashish Vaswani", "Noam Shazeer"],
    "authorText": "Ashish Vaswani, Noam Shazeer",
    "storageLocation": "papers/10001/original/",
    "uploadTime": "2026-05-12T10:30:00.123+08:00",
    "parseStatus": "PARSING"
  },
  "traceId": "9e37d2f928b44f87",
  "timestamp": "2026-05-12T10:30:00.123+08:00"
}
```

业务规则：

- 文件扩展名必须为 `.pdf`，且 MIME 类型应为 `application/pdf`。
- 建议单文件大小上限为 200 MB；超过限制返回 `FILE_TOO_LARGE`。
- 后端计算文件 MD5 并写入 `papers.paper_md5`。
- 查重优先级：
  1. `paper_md5` 完全一致：重复文件，返回 `DUPLICATE_PAPER`。
  2. `doi` 完全一致：疑似同一论文，返回 `DUPLICATE_PAPER`。
  3. `title + 第一作者 + year` 高度一致：疑似重复文献，返回 `DUPLICATE_PAPER`。
- 命中重复规则时，后端不写入新的 `papers` 记录，不启动解析。
- 未命中重复规则时，写入 `papers` 主表：
  - `paper_md5`：文件 MD5。
  - `title`：表单标题、解析标题或文件名兜底。
  - `authors_json`：作者数组 JSON。
  - `keywords_json`：关键词数组 JSON。
  - `language`：表单语言或默认 `en`。
  - `year`、`venue`、`doi`：表单或后续解析补全值。
  - `submission_time`：当前服务端时间。
  - `parse_status`：初始为 `1`，即 `PARSING`。
- MinerU 解析成功后更新 `parse_status = 2`；解析失败时更新 `parse_status = 3`。

常见错误：

| HTTP 状态 | code | 说明 |
|---:|---|---|
| 400 | `INVALID_FILE_TYPE` | 文件不是 PDF |
| 400 | `INVALID_JSON_FIELD` | `authorsJson` / `keywordsJson` 不是合法 JSON 数组 |
| 413 | `FILE_TOO_LARGE` | 文件超过大小限制 |
| 409 | `DUPLICATE_PAPER` | 重复或疑似重复文献，上传被拒绝 |
| 502 | `MINERU_PARSE_FAILED` | MinerU 解析失败，文献记录已创建但解析状态为失败 |
| 500 | `UPLOAD_FAILED` | 上传流程内部失败 |

重复错误响应示例：

```json
{
  "code": "DUPLICATE_PAPER",
  "message": "检测到重复或疑似重复文献，上传已取消",
  "data": {
    "duplicateReason": "PDF_MD5_MATCHED",
    "existingPaper": {
      "paperId": 10001,
      "title": "Attention Is All You Need",
      "authors": ["Ashish Vaswani", "Noam Shazeer"],
      "paperMd5": "1f3870be274f6c49b3e31a0c6728957f",
      "uploadTime": "2026-05-12T10:30:00.123+08:00"
    }
  },
  "traceId": "9e37d2f928b44f87",
  "timestamp": "2026-05-12T10:31:00.123+08:00"
}
```

`duplicateReason` 可选值：

| 值 | 说明 |
|---|---|
| `PDF_MD5_MATCHED` | 文件 MD5 完全一致 |
| `DOI_MATCHED` | DOI 完全一致 |
| `TITLE_AUTHOR_YEAR_SIMILAR` | 标题、第一作者、年份高度一致 |

## 6. 文献查询接口

### 6.1 查询文献列表

```http
GET /api/documents
```

用于文献库页面表格展示、搜索、筛选和分页。

查询参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `keyword` | string | 否 | 搜索文件名、标题、作者、DOI、期刊 / 会议 |
| `parseStatus` | string | 否 | 解析状态：`PENDING` / `PARSING` / `PARSED` / `FAILED` |
| `year` | integer | 否 | 发表年份 |
| `venue` | string | 否 | 期刊 / 会议 / 出版源 |
| `page` | integer | 否 | 页码，默认 1 |
| `pageSize` | integer | 否 | 每页数量，默认 20 |

成功响应：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {
    "items": [
      {
        "paperId": 10001,
        "paperMd5": "1f3870be274f6c49b3e31a0c6728957f",
        "fileName": "attention.pdf",
        "fileType": "PDF",
        "title": "Attention Is All You Need",
        "authors": ["Ashish Vaswani", "Noam Shazeer"],
        "authorText": "Ashish Vaswani, Noam Shazeer",
        "storageLocation": "papers/10001/original/",
        "uploadTime": "2026-05-12T10:30:00.123+08:00",
        "year": 2017,
        "venue": "NeurIPS",
        "doi": "10.5555/3295222.3295349",
        "parseStatus": "PARSED"
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 1,
    "hasNext": false
  },
  "traceId": "9e37d2f928b44f87",
  "timestamp": "2026-05-12T10:40:00.123+08:00"
}
```

列表排序建议：

- 默认按 `submission_time DESC` 排序。
- `submission_time` 为空的历史数据排在最后。

前端字段对应：

| 前端列 | 接口字段 |
|---|---|
| 文献文件名称 | `fileName`、`fileType` |
| 标题名称 | `title` |
| 作者姓名 | `authorText` |
| 存储位置 | `storageLocation` |
| 上传日期 | `uploadTime` |
| 操作 | 前端根据 `paperId` 发起详情查看等动作 |

### 6.2 查询文献详情

```http
GET /api/documents/{paperId}
```

用于进入文献详情页或列表操作中的预览 / 查看详情。

路径参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `paperId` | long | 是 | 论文 ID |

成功响应：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {
    "paperId": 10001,
    "paperMd5": "1f3870be274f6c49b3e31a0c6728957f",
    "fileName": "attention.pdf",
    "fileType": "PDF",
    "title": "Attention Is All You Need",
    "authors": ["Ashish Vaswani", "Noam Shazeer"],
    "authorText": "Ashish Vaswani, Noam Shazeer",
    "keywords": ["transformer", "attention"],
    "language": "en",
    "year": 2017,
    "venue": "NeurIPS",
    "doi": "10.5555/3295222.3295349",
    "storageLocation": "papers/10001/original/",
    "uploadTime": "2026-05-12T10:30:00.123+08:00",
    "parseStatus": "PARSED"
  },
  "traceId": "9e37d2f928b44f87",
  "timestamp": "2026-05-12T10:40:00.123+08:00"
}
```

错误：

| HTTP 状态 | code | 说明 |
|---:|---|---|
| 404 | `PAPER_NOT_FOUND` | 论文不存在 |

## 7. 错误码

| code | HTTP 状态 | 说明 |
|---|---:|---|
| `SUCCESS` | 200 | 成功 |
| `BAD_REQUEST` | 400 | 请求参数错误 |
| `INVALID_FILE_TYPE` | 400 | 不支持的文件类型 |
| `INVALID_JSON_FIELD` | 400 | JSON 字段格式错误 |
| `FILE_TOO_LARGE` | 413 | 文件过大 |
| `PAPER_NOT_FOUND` | 404 | 论文不存在 |
| `DUPLICATE_PAPER` | 409 | 重复或疑似重复文献 |
| `MINERU_PARSE_FAILED` | 502 | MinerU API 解析失败 |
| `UPLOAD_FAILED` | 500 | 上传流程内部失败 |
| `INTERNAL_ERROR` | 500 | 服务端未知错误 |

错误响应示例：

```json
{
  "code": "INVALID_JSON_FIELD",
  "message": "authorsJson 不是合法 JSON 数组",
  "data": {
    "field": "authorsJson"
  },
  "traceId": "9e37d2f928b44f87",
  "timestamp": "2026-05-12T10:30:00.123+08:00"
}
```

## 8. 核心业务流程

### 8.1 本地上传成功流程

```text
POST /api/documents/upload
  -> 校验 PDF 类型和大小
  -> 计算 paper_md5
  -> 按 paper_md5 / DOI / title + 第一作者 + year 查重
  -> 未命中重复
  -> 写入 papers:
       paper_md5
       title
       authors_json
       keywords_json
       language
       year
       venue
       doi
       submission_time
       parse_status = 1
  -> 调用 MinerU 解析
  -> 解析成功后更新 title / authors_json / keywords_json / year / venue / doi 等可补全字段
  -> 更新 parse_status = 2
  -> 返回上传成功
```

### 8.2 重复文献拦截流程

```text
POST /api/documents/upload
  -> 校验 PDF 类型和大小
  -> 计算 paper_md5
  -> 按 paper_md5 / DOI / title + 第一作者 + year 查重
  -> 命中重复或疑似重复
  -> 返回 409 DUPLICATE_PAPER
  -> 不写入新的 papers 记录
  -> 不启动 MinerU 解析
```

### 8.3 MinerU 解析失败流程

```text
POST /api/documents/upload
  -> 已写入 papers，parse_status = 1
  -> MinerU API failed
  -> 更新 parse_status = 3
  -> 返回 MINERU_PARSE_FAILED 或返回上传成功但状态为 FAILED
```

第一版建议：如果论文记录已创建但解析失败，返回 `502 MINERU_PARSE_FAILED`，并在错误响应 `data.paperId` 中带上已创建的论文 ID，便于前端刷新列表或提示用户。

## 9. 前端接入建议

### 9.1 文献库页面

推荐接口：

- `GET /api/documents`：加载表格。
- `POST /api/documents/upload`：上传 PDF。
- `GET /api/documents/{paperId}`：进入详情页。

上传交互建议：

- 前端可以继续做 PDF / DOC / DOCX 本地预览，但真正提交后端时只提交 PDF。
- 前端可先做文件类型和大小校验，但后端必须重复校验。
- 上传成功后刷新 `GET /api/documents`。
- 收到 `DUPLICATE_PAPER` 时直接提示上传失败，并展示 `data.existingPaper` 中的已存在文献信息。
- 收到 `MINERU_PARSE_FAILED` 时提示解析失败；如果响应中存在 `data.paperId`，可允许用户在列表中看到失败状态。

### 9.2 文献列表字段

前端 `LibraryView.vue` 当前表格列与接口字段的建议绑定关系：

| 页面列 | 建议绑定 |
|---|---|
| 文献文件名称 | `fileType` 徽标 + `fileName` |
| 标题名称 | `title` |
| 作者姓名 | `authorText` |
| 存储位置 | `storageLocation` |
| 上传日期 | `uploadTime` 格式化为 `YYYY-MM-DD` |
| 操作 | `paperId` |

## 10. 后端实现建议

### 10.1 Controller 划分

```text
DocumentController
  POST /api/documents/upload
  GET  /api/documents
  GET  /api/documents/{paperId}
```

### 10.2 Service 调用边界

```text
DocumentController
  -> DocumentImportService
  -> PaperService

DocumentImportService
  -> DuplicatePaperService
  -> MinerUParseService
  -> PaperService
```

### 10.3 幂等性和查重建议

- `paper_md5` 是上传接口的主要幂等依据，数据库已设置唯一约束。
- 后端需要捕获 `paper_md5` 唯一键冲突，并统一转换为 `DUPLICATE_PAPER`。
- DOI 查重建议忽略大小写并去除 URL 前缀，例如 `https://doi.org/`。
- 标题相似查重可以先做保守实现：标题标准化后完全一致、第一作者一致、年份一致才判重；避免误杀。

## 11. OpenAPI 片段示例

```yaml
paths:
  /api/documents/upload:
    post:
      summary: 上传本地 PDF，重复文献直接报错
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                title:
                  type: string
                authorsJson:
                  type: string
                keywordsJson:
                  type: string
                language:
                  type: string
                  default: en
                year:
                  type: integer
                venue:
                  type: string
                doi:
                  type: string
      responses:
        "200":
          description: 上传成功
        "400":
          description: 参数或文件类型错误
        "409":
          description: 重复或疑似重复文献
        "413":
          description: 文件过大
        "502":
          description: MinerU 解析失败
  /api/documents:
    get:
      summary: 查询文献列表
  /api/documents/{paperId}:
    get:
      summary: 查询文献详情
```

## 12. 第一版落地优先级

建议按以下顺序实现接口：

1. `POST /api/documents/upload`
2. `GET /api/documents`
3. `GET /api/documents/{paperId}`

这样可以先跑通“上传 PDF -> 查重 -> 写入 papers -> MinerU 解析 -> 列表 / 详情展示”的最小闭环。
