# ScholarEase 问答功能实现流程设计

## 1. 目标与边界

本文件描述 ScholarEase 论文问答功能的第一版实现流程。该功能面向“中文提问、英文论文召回、中文回答并附引用”的 RAG 场景，要求使用 Redis 保存多轮对话历史，使用 LangChain4j 作为 agent / RAG 编排框架，召回参考文档时使用向量召回 + BM25 召回 + rerank 精排，并在检索阶段处理中英文翻译与 query rewrite。

当前只做方案设计，不写业务代码。

第一版目标：

1. 支持用户在前端问答页输入中文问题。
2. 支持 session 级多轮对话，历史消息和当前论文上下文存入 Redis。
3. 使用 LangChain4j 组织 ChatModel、Memory、Retriever、Prompt 和 Tool 调用边界。
4. 使用 Elasticsearch 已有 `scholarease_base` 索引完成 dense_vector 向量召回和 BM25 关键词召回。
5. 使用翻译 / 改写结果提升中文问题检索英文论文的 BM25 命中率。
6. 使用 reranker 对混合召回候选 chunk 精排。
7. 回答必须用中文，并且每个关键结论带 paper、section、page、chunk 等引用来源。
8. 检索、重排、回答过程保留日志，便于后续构建检索调试页和评估集。

不纳入第一版：

1. 不引入登录、权限、组织空间和复杂安全模型。
2. 不单独引入 Qdrant，继续使用当前 Elasticsearch 保存 `textContent` 和 `vector`。
3. 不把全文 chunk 写入 MySQL；第一版继续以 ES 中的 `paperMd5 + chunkIndex + sectionPath + pageStart/pageEnd` 作为引用定位。
4. 不做 GraphRAG、citation graph、claim-evidence 自动抽取。

## 2. 当前项目事实

根据当前项目代码，第一版问答功能应基于以下现状设计：

| 模块 | 当前状态 | 对问答功能的影响 |
|---|---|---|
| 后端框架 | Java 17 + Spring Boot 3.5.14 | 问答 API 继续放在 `backend` 模块 |
| Redis | 已有 `spring-boot-starter-data-redis` 和 `spring.data.redis` 配置 | 可直接设计 Redis 会话存储 |
| LangChain4j | `pom.xml` 尚未引入 LangChain4j | 第一版需要新增依赖与模型适配层 |
| Elasticsearch | 已有 ES Java Client 使用，默认索引名配置为 `scholarease_bge_m3` | 检索服务应复用现有 ES 客户端和索引配置；旧 `scholarease_base` 如为 2048 维不再复用 |
| ES mapping | `textContent/rawText` 使用 IK 分词，`vector` 为 `dense_vector`，维度 1024 | 当前项目默认使用 DeepInfra `BAAI/bge-m3`，与架构文档推荐一致 |
| Embedding | 已有 `EmbeddingClient` 和 OpenAI-compatible embedding 客户端 | query embedding 可复用该接口，但要保证与 ES 中 chunk embedding 同模型同维度 |
| 向量索引 | 已有 Kafka 异步向量化、`PaperVectorDocument`、`PaperChunkBuildService` | 问答检索直接读取已写入 ES 的 chunk 文档 |
| 文献定位 | `PaperVectorDocument` 有 `paperMd5`、`chunkIndex`、`sectionPath`、`pageStart/pageEnd`、`sourceObjectKey` | 引用可先用这些字段生成，后续再补 `paperId/chunkId` |
| 前端问答页 | `ChatAssistantView.vue` 目前是静态示例 Markdown 渲染 | 需要接入真实问答 API 和流式响应 |
| 模型设置 | 前端已有 provider、adapter、baseUrl、apiKey、modelName 等状态 | 可作为后端 LangChain4j ChatModel 配置来源，或先以后端配置为准 |

项目当前默认使用 DeepInfra `BAAI/bge-m3`，dense embedding 维度为 1024，默认 Elasticsearch 索引为 `scholarease_bge_m3`。第一版问答必须以已建索引的向量维度和模型为准，否则 query embedding 与 chunk vector 不兼容。如果本地已有旧的 `scholarease_base` / `text-embedding-v4` / 2048 维 Elasticsearch 索引，不应继续混用；可以保留旧索引，改用新索引名并用 bge-m3 重新向量化所有论文 chunk。

## 3. 总体链路

```text
用户中文问题
  -> 前端 ChatAssistantView
  -> POST /api/qa/chat 或 SSE /api/qa/chat/stream
  -> ChatController
  -> ConversationService 读取 Redis 历史和 session context
  -> LangChain4j QaAgent 编排
  -> QueryUnderstandingService 生成独立问题、实体、过滤条件
  -> QueryTranslationService 生成英文 BM25 query 和关键词
  -> HybridRetrievalService
       -> EmbeddingClient 生成 query vector
       -> Elasticsearch kNN 向量召回
       -> Elasticsearch BM25 关键词召回
       -> RRF / 加权融合
  -> RerankService 精排候选 chunk
  -> ContextExpansionService 补邻近 chunk / 同 section 片段
  -> PromptBuilder 组织证据上下文
  -> LangChain4j ChatModel 生成中文回答
  -> CitationAssembler 整理引用来源
  -> ConversationService 写回 Redis
  -> 前端展示回答、引用卡片和可展开证据
```

推荐第一版采用“确定性 RAG 编排 + LangChain4j agent 外壳”的方式：检索必须由后端流程显式执行，不能完全交给 LLM 自主决定是否调用检索工具。这样可以保证论文问答始终有证据来源，后续再逐步把检索、摘要、对比、解释等能力封装为 LangChain4j Tool。

## 4. 后端模块划分

建议新增或扩展以下模块：

| 模块 | 职责 |
|---|---|
| `qa` / `answer` | 问答入口、响应 DTO、引用 DTO、流式输出包装 |
| `conversation` | Redis 会话、消息历史、当前论文上下文、历史摘要 |
| `agent` | LangChain4j agent 编排，管理 ChatModel、Memory、Prompt、Tool |
| `modelgateway` | 根据 provider/baseUrl/modelName 构建 LangChain4j ChatModel 与 StreamingChatModel |
| `query` | 多轮问题改写、语言识别、英文翻译、关键词抽取、过滤条件解析 |
| `retrieval` | ES 向量召回、BM25 召回、RRF 融合、候选去重 |
| `rerank` | reranker 模型调用、重排分数标准化、topN 截断 |
| `citation` | 引用编号、来源合并、证据片段格式化 |
| `evaluation` | 检索日志、rerank 日志、回答日志、失败案例记录 |

已有 `EmbeddingClient`、`PaperVectorSearchIndexService`、`PaperVectorDocument`、`ScholarEaseElasticsearchProperties` 应尽量复用。当前 `PaperVectorSearchIndexService` 只定义写入和删除，问答功能可新增只读检索接口，例如 `PaperRetrievalSearchService`，避免把索引写入职责和检索职责混在一个接口里。

## 5. Redis 对话历史设计

Redis 只保存短期会话状态，不作为长期审计数据库。后续如需长期留存，可把会话摘要写入 MySQL。

推荐 key：

```text
chat:session:{sessionId}:messages
chat:session:{sessionId}:context
chat:session:{sessionId}:summary
chat:session:{sessionId}:last-retrieval
```

### 5.1 messages

类型建议使用 Redis List，按时间追加 JSON。

单条消息结构：

```json
{
  "messageId": "uuid",
  "role": "user | assistant | system",
  "content": "消息正文",
  "createdAt": "2026-05-18T20:30:00+08:00",
  "citations": [
    {
      "citationId": "S1",
      "paperMd5": "xxx",
      "chunkIndex": 12,
      "title": "paper title",
      "sectionPath": "Methods / Training",
      "pageStart": 4,
      "pageEnd": 5,
      "score": 0.82
    }
  ]
}
```

保留策略：

1. 每个 session 默认保留最近 20 轮消息。
2. 超出窗口后，将较早内容压缩为 `chat:session:{sessionId}:summary`。
3. TTL 默认 7 到 30 天，个人本地场景可配置为不过期。

### 5.2 context

类型建议使用 Redis Hash 或 JSON 字符串。

保存内容：

```json
{
  "currentPaperMd5": "用户当前聚焦的论文",
  "currentPaperTitle": "论文标题",
  "activeFilters": {
    "paperMd5List": ["xxx"],
    "yearRange": [2018, 2026],
    "authors": ["Vaswani"]
  },
  "lastUserQuestion": "上一轮用户问题",
  "lastStandaloneQuestion": "上一轮独立问题",
  "lastCitationIds": ["S1", "S2"]
}
```

多轮省略指代时，例如“它的实验设置是什么？”、“这篇论文有什么不足？”，优先从 Redis context 中补全当前论文过滤条件。

## 6. LangChain4j 编排方案

### 6.1 ChatModel 接入

第一版优先支持 OpenAI-compatible 模型，因为前端已有 DeepSeek、Qwen、OpenAI、MiniMax、SiliconFlow、本地模型等配置，且 LangChain4j 对 OpenAI-compatible API 适配成熟。

推荐模型配置来源：

1. 第一阶段以后端 `application.yml` / 环境变量为准，保证服务端可控。
2. 第二阶段允许前端模型设置随请求提交，但后端要做白名单校验，避免任意 baseUrl 和 apiKey 被滥用。

ChatModel 职责：

1. 生成多轮问题的 standalone question。
2. 生成英文 query rewrite / BM25 keywords。
3. 基于检索证据生成最终中文回答。
4. 必要时压缩长期会话摘要。

### 6.2 Agent 形态

推荐第一版定义一个 `ScholarEaseQaAgent`，但内部流程不要让 LLM 自由跳步：

```text
QaAgentService.answer(request)
  -> load memory
  -> build standalone question
  -> translate / rewrite query
  -> retrieve evidence
  -> rerank evidence
  -> build grounded prompt
  -> call LangChain4j ChatModel
  -> persist memory
```

LangChain4j 在这里负责：

1. ChatModel / StreamingChatModel 统一封装。
2. ChatMemory / ChatMemoryStore 与 Redis 对接。
3. PromptTemplate 管理系统提示词、改写提示词和回答提示词。
4. 后续把 `paperSearch`、`paperCompare`、`paperSummarize` 等能力注册为 Tool。

## 7. Query 理解、翻译与改写流程

中文问题检索英文论文时，向量召回和 BM25 召回应走不同策略：

1. 向量召回：使用与索引一致的 embedding 模型直接对中文 standalone question 生成 query vector。如果当前 embedding 模型跨语言效果不足，可改用英文译文生成 query vector，或中文 + 英文各召回一次。
2. BM25 召回：必须使用英文译文、英文关键词和保留的专有名词检索 `textContent/rawText/title/sectionPath/authorText`。

### 7.1 输入规范化

输入：

```text
原始问题 + Redis 历史摘要 + 最近 N 轮消息 + session context
```

输出：

```json
{
  "standaloneQuestionZh": "Transformer 论文中 multi-head attention 的作用是什么？",
  "intent": "concept_explanation",
  "language": "zh",
  "paperFilters": {
    "paperMd5List": [],
    "titleKeywords": ["Transformer"]
  },
  "mustHaveTerms": ["multi-head attention", "Transformer"],
  "sectionHints": ["method", "model", "architecture"]
}
```

### 7.2 翻译与 BM25 改写

翻译要求：

1. 中文自然语言翻译为英文检索句。
2. 专有名词、模型名、数据集、指标名、公式符号保持原文。
3. 输出短 query 和关键词列表两种形态。
4. 可补充同义词，但不能引入不存在的论文事实。

输出示例：

```json
{
  "queryZh": "Transformer 论文中 multi-head attention 的作用是什么？",
  "queryEn": "What is the role of multi-head attention in the Transformer paper?",
  "bm25Keywords": [
    "multi-head attention",
    "Transformer",
    "attention heads",
    "representation subspaces",
    "scaled dot-product attention"
  ],
  "exactTerms": ["multi-head attention", "Transformer"]
}
```

### 7.3 路由策略

| 意图 | 检索侧重点 |
|---|---|
| 论文概述 | title、abstract、introduction、conclusion chunk 优先 |
| 方法解释 | method、architecture、model、approach section 优先 |
| 实验设置 | experiment、dataset、implementation、training、evaluation section 优先 |
| 指标和结果 | results、table、evaluation、ablation chunk 优先 |
| 对比多篇论文 | 每篇论文分别召回，再交给回答模型横向比较 |
| 追问上一轮 | 从 Redis context 继承 paper filter 和最近引用 |

## 8. 混合召回流程

### 8.1 向量召回

输入：

```text
standaloneQuestionZh 或 queryEn
```

流程：

1. 调用现有 `EmbeddingClient.embed()` 生成 query vector。
2. 使用 ES kNN 在 `vector` 字段召回 topK。
3. 添加 metadata filter：`paperMd5`、year、authors、language、chunkType、sectionPath 等。
4. 返回候选 `PaperEvidence`。

默认参数：

```text
vector_top_k = 40
num_candidates = 200
```

注意：query embedding 的模型、维度必须与 ES chunk vector 一致。当前项目为 DeepInfra `BAAI/bge-m3` + 1024 维；如果曾经用 `text-embedding-v4` + 2048 维写入过旧索引，需要重建索引后再检索。

### 8.2 BM25 召回

输入：

```text
queryEn + bm25Keywords + exactTerms
```

检索字段建议：

```text
textContent^2
rawText^3
title^2
sectionPath^1.5
authorText
venue
doi
```

策略：

1. 英文 query 使用 `match` / `multi_match`。
2. `exactTerms` 使用 phrase query 提权。
3. 方法名、模型名、数据集名、指标名使用 `should` 增强。
4. 如果用户指定论文，必须加 `paperMd5` filter。

默认参数：

```text
bm25_top_k = 40
```

### 8.3 结果融合

向量召回和 BM25 召回各自返回候选后，按 `paperMd5 + chunkIndex + contentHash` 去重。

第一版优先使用 RRF：

```text
rrf_score = 1 / (k + vector_rank) + 1 / (k + bm25_rank)
```

默认：

```text
rrf_k = 60
merged_top_k = 60
```

如果后续需要按意图调权：

```text
概念解释 / 机制 / 为什么：vector_weight 更高
模型名 / 数据集 / 指标 / 作者 / DOI：bm25_weight 更高
```

融合输出的候选结构：

```json
{
  "paperMd5": "xxx",
  "chunkIndex": 8,
  "title": "Attention Is All You Need",
  "rawText": "原始 chunk 文本",
  "textContent": "带论文元数据前缀的文本",
  "sectionPath": "3 Model Architecture / 3.2 Attention",
  "pageStart": 4,
  "pageEnd": 5,
  "vectorRank": 3,
  "bm25Rank": 12,
  "rrfScore": 0.028,
  "sourceObjectKey": "uploads/{traceId}/mineru/content_list_v2.json"
}
```

## 9. Rerank 精排流程

rerank 用于判断“候选 chunk 是否真的能回答问题”，降低纯向量或纯关键词带来的弱相关结果。

输入：

```text
query: standaloneQuestionZh 或 queryEn
passages: merged top 60 的 rawText/textContent
```

建议：

1. 如果使用 `bge-reranker-v2-m3`，query 可用中文，passage 可用英文。
2. 如果 reranker 服务不支持跨语言，query 使用英文译文。
3. passage 优先传 `rawText`，同时附短 metadata，例如 title、sectionPath、page，避免元数据前缀过长干扰正文相关性。

默认参数：

```text
rerank_input_top_k = 60
rerank_output_top_k = 10
final_context_chunks = 4-8
```

精排后过滤：

1. 低于阈值的候选不进入回答上下文。
2. 同一论文同一 section 命中过多时做去冗余，避免上下文被一篇论文占满。
3. 表格 / 公式 / 图片 caption 可保留更高优先级，因为它们常包含实验结果和指标。

## 10. Context Expansion

rerank topN 之后，需要扩展上下文，防止单个 chunk 不完整。

第一版可用 ES 当前字段做轻量扩展：

1. 对每个最终 chunk，尝试按同一 `paperMd5` 查询 `chunkIndex - 1` 和 `chunkIndex + 1`。
2. 如果当前 chunk 是 table / figure / equation，优先补同页前后 paragraph。
3. 如果 sectionPath 相同且 chunkIndex 相邻，合并为一个 evidence group。
4. 扩展后的上下文仍需控制总 token，不超过模型上下文预算。

输出给回答模型的 evidence 格式：

```text
[S1]
Paper: Attention Is All You Need
Section: 3.2 Attention
Page: 4-5
Chunk: md5-xxx#8
Text:
...

[S2]
Paper: ...
```

## 11. 回答生成流程

系统提示词核心约束：

1. 你是 ScholarEase 的论文问答助手。
2. 只能依据给定 evidence 回答，不允许编造论文事实。
3. 使用中文回答。
4. 每个关键结论后必须引用证据编号，例如 `[S1]`、`[S2]`。
5. 如果证据不足，要明确说“当前检索到的证据不足以回答”。
6. 不要输出没有来源的论文结论。
7. 对多个来源有冲突的情况，分别说明来源差异。

回答结构建议：

```text
直接回答用户问题。

关键依据：
1. ...
2. ...

证据不足或注意事项：
...
```

前端展示时：

1. 正文保留 Markdown。
2. `[S1]` 可点击展开引用卡片。
3. 引用卡片显示 title、authors、year、sectionPath、pageStart/pageEnd、chunkIndex、rawText 摘要。
4. 后续可增加“跳转论文详情 / 定位 PDF 页面”。

## 12. API 流程建议

### 12.1 创建会话

```text
POST /api/qa/sessions
```

返回：

```json
{
  "sessionId": "uuid",
  "createdAt": "2026-05-18T20:30:00+08:00"
}
```

### 12.2 发送问题

普通响应：

```text
POST /api/qa/chat
```

流式响应：

```text
POST /api/qa/chat/stream
```

请求：

```json
{
  "sessionId": "uuid",
  "message": "这篇论文的核心方法是什么？",
  "paperMd5List": ["可选，用户当前选中的论文"],
  "modelConfig": {
    "provider": "deepseek",
    "adapter": "OpenAiStreamingChatModel",
    "baseUrl": "https://api.deepseek.com/v1",
    "modelName": "deepseek-chat",
    "temperature": 0.2,
    "maxTokens": 4096
  }
}
```

响应：

```json
{
  "sessionId": "uuid",
  "messageId": "uuid",
  "answer": "中文回答...",
  "citations": [
    {
      "citationId": "S1",
      "paperMd5": "xxx",
      "chunkIndex": 8,
      "title": "Attention Is All You Need",
      "sectionPath": "3.2 Attention",
      "pageStart": 4,
      "pageEnd": 5,
      "rawTextPreview": "..."
    }
  ],
  "debug": {
    "standaloneQuestionZh": "...",
    "queryEn": "...",
    "vectorHitCount": 40,
    "bm25HitCount": 40,
    "rerankInputCount": 60,
    "finalEvidenceCount": 6
  }
}
```

`debug` 第一版可只在后端日志中保留，前端检索调试页实现后再开放。

## 13. 检索日志与评估

建议每次问答生成一个 `qaTraceId`，贯穿以下日志：

1. 用户原始问题。
2. Redis 读取到的历史摘要和 current paper context。
3. standalone question。
4. queryEn、bm25Keywords、exactTerms。
5. vector 召回 topK：paperMd5、chunkIndex、score、rank。
6. BM25 召回 topK：paperMd5、chunkIndex、score、rank。
7. RRF 融合结果。
8. rerank 分数。
9. 最终 evidence。
10. LLM 回答与引用编号。

第一版可以先写应用日志；后续检索调试页需要结构化保存到 MySQL 表，例如：

```text
qa_sessions
qa_messages
qa_retrieval_logs
qa_rerank_logs
qa_citation_logs
```

## 14. 错误与降级策略

| 场景 | 处理策略 |
|---|---|
| Redis 不可用 | 返回明确错误；不建议静默退化为无历史，因为多轮语义会变差 |
| Embedding API 失败 | 问答失败并提示“检索向量生成失败”，记录 qaTraceId |
| BM25 无结果 | 继续使用向量结果，但 debug 日志记录 BM25 miss |
| 向量无结果 | 继续使用 BM25 结果，但提示可能只做关键词匹配 |
| reranker 不可用 | 降级为 RRF topN，但回答中保持证据约束 |
| LLM 失败 | 不写 assistant 消息，只保留 user 消息和失败日志 |
| 检索结果低于阈值 | 回答“证据不足”，不要让模型自由发挥 |
| query 翻译失败 | 使用原中文 query 做向量召回，同时用原文和专有名词做 BM25 |

## 15. 第一版实施顺序

1. 引入 LangChain4j 依赖，建立 `modelgateway`，支持 OpenAI-compatible ChatModel / StreamingChatModel。
2. 建立 `ConversationService`，用 Redis 保存 messages、context、summary。
3. 新增问答 DTO 和 `ChatController`，先支持非流式 `POST /api/qa/chat`。
4. 实现 `QueryUnderstandingService`：基于历史生成 standalone question 和 paper filters。
5. 实现 `QueryTranslationService`：生成英文 query、BM25 keywords、exact terms。
6. 新增 ES 只读检索服务：实现 kNN vector search、BM25 multi_match search。
7. 实现 RRF 融合和候选去重。
8. 接入 `RerankService`；如果模型服务暂未准备好，先预留接口并用 RRF topN 降级。
9. 实现 `ContextExpansionService`，按 `paperMd5 + chunkIndex` 补前后 chunk。
10. 实现 `AnswerService` / `QaAgentService`，用 LangChain4j PromptTemplate 生成带引用中文回答。
11. 前端 `ChatAssistantView.vue` 接入问答 API，替换静态示例消息。
12. 增加结构化检索日志，为后续检索调试页和评估集做准备。
13. 增加单元测试与集成测试：Redis memory、query rewrite、ES DSL 构造、RRF、citation assembly、证据不足场景。

## 16. 关键取舍

1. LangChain4j 做编排，不接管核心检索数据模型。检索日志、chunk provenance、引用格式仍由项目内部控制。
2. 第一版以现有 ES mapping 和 DeepInfra `BAAI/bge-m3` 1024 维为准。不同 embedding 模型和维度不应混用到同一个索引。
3. 中文问题可以直接做向量召回，但 BM25 必须通过英文翻译和关键词改写增强。
4. rerank 只处理融合后的 top 60 左右候选，避免响应时间失控。
5. 回答模型必须被 evidence 约束；检索不到足够证据时宁可回答“不足”，也不要生成无来源结论。

## 17. 推荐的第一版默认参数

```text
history_recent_turns = 20
redis_session_ttl_days = 30
vector_top_k = 40
vector_num_candidates = 200
bm25_top_k = 40
rrf_k = 60
merged_top_k = 60
rerank_top_k = 10
final_context_chunks = 4-8
temperature = 0.2
max_tokens = 4096
```

## 18. 最终一句话流程

ScholarEase 问答功能应在现有论文向量索引基础上，由前端提交中文问题，后端从 Redis 读取多轮上下文，使用 LangChain4j 组织 query 改写、英文翻译、ES 向量召回、ES BM25 召回、RRF 融合、rerank 精排和 evidence prompt，最后由 LLM 生成带引用的中文回答，并把用户问题、助手回答、引用来源和当前 session context 写回 Redis。
