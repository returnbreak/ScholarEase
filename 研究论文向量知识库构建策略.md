# 即插即用的向量知识库构建策略：针对研究论文文献

比较推荐的总原则是：**不要把论文 PDF 直接切块丢进向量库**；更好的“即插即用”路线是：

```text
结构化解析 → 论文语义分块 → 多粒度索引 → 混合检索 → rerank → 带引用回答
```

对研究论文来说，标题、摘要、章节、图表、公式、引用、实验设置、limitations 都是强信号，丢掉这些结构会显著影响检索质量。

---

## 1. 最推荐的基线方案：结构化 RAG，而不是普通向量库

可以从这个 pipeline 起步：

```text
PDF / arXiv / DOI
  → 结构化解析：GROBID / JATS / LaTeX / HTML
  → 规范化元数据：title, authors, year, venue, DOI, arXiv id, references
  → 章节级分块：abstract / intro / method / experiment / result / discussion / limitation
  → chunk 增强：标题 + 章节路径 + 局部文本 + 引用上下文
  → dense embedding + sparse/BM25
  → metadata filter
  → hybrid retrieval
  → cross-encoder / late-interaction rerank
  → parent/neighbor context expansion
  → 回答时强制引用 paper_id + section + page / paragraph
```

GROBID 很适合做论文 PDF 的第一层结构化解析，因为它专门把科学/技术 PDF 解析成结构化 TEI XML，用于文本挖掘和语义分析。官方也建议保留 TEI 作为较完整的结构化结果，因为 Markdown/JSON 投影会丢失一些布局、坐标和书目信息。

参考：[GROBID Principles](https://grobid.readthedocs.io/en/latest/Principles/)

---

## 2. 策略一：论文感知的 chunk，而不是固定长度 chunk

论文文献库最常见的坑是：

```text
1000 tokens + 200 overlap
```

一把梭。更稳的做法是：**每个 chunk 都带上上下文前缀**。

例如：

```text
Paper: "Attention Is All You Need"
Year: 2017
Section: 3.2.1 Scaled Dot-Product Attention
Chunk:
...
```

### 推荐分块方式

| 内容类型 | 推荐粒度 | 备注 |
|---|---:|---|
| title + abstract | 单独建 paper-level chunk | 适合“这篇论文讲什么” |
| introduction / related work | 500–900 tokens | 适合背景和动机 |
| method | 400–700 tokens | 保留公式前后解释 |
| experiment / results | 300–700 tokens | 尽量别拆断指标、数据集和结论 |
| limitations / discussion | 300–700 tokens | 对综述和批判性问题很重要 |
| references | 不建议和正文混切 | 应单独做 citation graph |
| figures / tables | 单独对象化 | 图表 caption、表格内容、页码、bbox 分开存 |

实际落地时可以做 **child chunk 检索 + parent chunk 返回**。

也就是小 chunk 用来提高命中率，命中后把所在段落、章节或相邻 chunk 一起送给模型。LlamaIndex 的 hierarchical parser 就是类似思路：它支持把文档切成多层级节点，例如 2048、512、128 token 的父子节点。

参考：[LlamaIndex Hierarchical Node Parser](https://docs.llamaindex.ai/en/stable/api_reference/node_parsers/hierarchical/)

---

## 3. 策略二：多粒度索引，一篇论文至少建 3 层

建议每篇论文建三类索引。

### 3.1 Paper-level index

用于回答：

```text
这篇论文解决什么问题？
有哪些贡献？
这篇和另一篇有什么区别？
```

字段包括：

```text
title
abstract
TL;DR
keywords
venue
year
authors
DOI
arXiv id
领域标签
```

### 3.2 Section / chunk-level index

用于回答：

```text
作者用了什么实验设置？
模型结构在哪里解释？
ablation 结果是什么？
```

这是主向量索引。

chunk 需要带：

```text
paper_id
section_path
section_type
page_range
paragraph_id
citation_mentions
```

### 3.3 Citation / graph-level index

用于回答：

```text
这条技术路线怎么演化？
哪些论文支持/反驳这个观点？
这篇论文主要依赖哪些前人工作？
```

这里不只是向量检索，而是把 references、in-text citation、DOI、Semantic Scholar / OpenAlex metadata 做成图。

GraphRAG 类方法尤其适合“全库级问题”，例如“这个领域的主要主题是什么”，因为普通 RAG 对整个语料的全局问题容易失效。GraphRAG 的思路是从文档中抽实体图，再生成社区摘要。

参考：[GraphRAG Research](https://graphrag.com/appendices/research/2404.16130/)

---

## 4. 策略三：hybrid retrieval 是默认配置，不要只用 dense vector

论文查询里有很多精确词：

```text
模型名
数据集名
指标名
基因名
化合物名
作者名
缩写
公式符号
```

纯向量检索经常会把这些“硬匹配”弄丢。

推荐默认检索方式：

```text
metadata filter
  + BM25 / sparse retrieval
  + dense vector retrieval
  + reciprocal rank fusion / weighted fusion
  + reranker
```

Weaviate 官方文档把 hybrid search 定义为结合向量搜索和 BM25 关键词搜索，并通过融合分数排序。Qdrant 的教程也给出 dense、sparse、late-interaction rerank 三路组合的检索架构。

参考：

- [Weaviate Hybrid Search Concepts](https://docs.weaviate.io/weaviate/concepts/search/hybrid-search)
- [Weaviate Hybrid Search](https://docs.weaviate.io/weaviate/search/hybrid)
- [Qdrant Hybrid Search / Advanced Retrieval](https://qdrant.tech/documentation/)

### 实用默认参数

```text
dense_top_k = 40
bm25_top_k = 40
merged_top_k = 60
rerank_top_k = 10–20
final_context = 4–8 chunks
```

### Query 类型和权重建议

| 用户问题类型 | 检索权重 |
|---|---|
| 某个方法 / 数据集 / 公式 / 作者 | BM25 权重更高 |
| 思想 / 假设 / 机制 / 区别 | dense vector 权重更高 |
| 精确术语 + 概念解释 | hybrid 平衡 |
| 综述型问题 | summary index + graph index |

---

## 5. 策略四：rerank 必须加，尤其是论文库

论文库里，第一阶段召回经常“看起来相关但其实不回答问题”。Rerank 的价值是把召回候选再按 query-passage 相关性重排。

Weaviate 文档也把 reranking 描述为第二阶段：先用较快方法取小候选集，再用更昂贵模型重新排序。

参考：[Weaviate Reranking Concepts](https://docs.weaviate.io/weaviate/concepts/reranking)

### 推荐三种 rerank 方案

| 场景 | 方案 |
|---|---|
| 轻量、便宜 | cross-encoder reranker |
| 追求检索质量 | ColBERT / late-interaction rerank |
| 已有大模型服务 | LLM relevance grading，只对 top 20 做 |

论文问答系统可以参考 PaperQA 的设计思想：它面向科学文献做全文检索、判断 source/passage 相关性，再基于检索结果回答并保留 provenance。

参考：[PaperQA2 paper](https://huggingface.co/papers/2312.07559)

---

## 6. 策略五：RAPTOR / 摘要树适合综述型问题

如果你的文献库不只是“查某个段落”，还要支持：

```text
总结 Transformer pruning 的主要路线
比较近五年 protein language model 的方法变化
找出这个领域的共识和争议
```

那就可以加一层 **summary index**：

```text
chunk summaries
  → section summaries
  → paper summaries
  → topic / cluster summaries
```

RAPTOR 的核心思想是递归地 embedding、聚类、总结文本块，构建不同抽象层级的树形索引，以避免只检索短连续片段导致缺少整体文档上下文。

参考：[RAPTOR, ICLR 2024](https://proceedings.iclr.cc/paper_files/paper/2024/hash/8a2acd174940dbca361a6398a4f9df91-Abstract-Conference.html)

### 推荐索引拆分

```text
原文 chunk index：回答细节问题
section summary index：回答章节级问题
paper summary index：回答单篇论文问题
cluster summary index：回答领域综述问题
```

这比单纯扩大 top_k 更稳定，因为它把“综述性问题”和“证据性问题”分开处理。

---

## 7. 策略六：图表、公式、表格要对象化存储

论文知识库里，很多关键答案在表格和图里。不要只存 caption。

### FigureObject

```text
figure_id
paper_id
figure_number
caption
page
bbox
linked_section
image_path
ocr_text
generated_description
```

### TableObject

```text
table_id
paper_id
table_number
caption
page
bbox
markdown_table / csv
linked_section
```

GROBID 可以检测 table location 并给出 bounding box，但官方说明结构化表格内容抽取需要 Tabula、Camelot 这类专门工具处理检测到的表格区域。

参考：[GROBID FAQ](https://grobid.readthedocs.io/en/latest/Frequently-asked-questions/)

### FormulaObject

公式建议至少存：

```text
formula_latex
surrounding_text_before
surrounding_text_after
section_path
```

GROBID 对公式和上下标也有限制：它能检测公式并保留部分内部格式信息，但并不总是把上下标序列化到 TEI XML 输出里。

参考：[GROBID FAQ](https://grobid.readthedocs.io/en/latest/Frequently-asked-questions/)

---

## 8. 一个可直接采用的数据模型

### papers

```text
paper_id
title
abstract
authors
year
venue
doi
arxiv_id
url
field
keywords
tldr
source_path
parse_status
created_at
```

### chunks

```text
chunk_id
paper_id
chunk_text
chunk_type: abstract / intro / method / result / discussion / limitation / caption / table / reference
section_title
section_path
section_level
page_start
page_end
paragraph_index
token_count
previous_chunk_id
next_chunk_id
parent_section_id
cited_paper_ids
embedding_model
content_hash
```

### sections

```text
section_id
paper_id
title
path
level
summary
page_start
page_end
```

### citations

```text
source_paper_id
target_paper_id
raw_citation_text
in_text_context
section_path
citation_intent: background / method / comparison / criticism / use
```

### figures / tables

```text
object_id
paper_id
type
number
caption
page
bbox
extracted_text
linked_chunk_id
```

---

## 9. 推荐的即插即用默认架构

### 9.1 小型个人文献库：几百到几千篇

```text
GROBID
+ SQLite/Postgres 存 metadata
+ Qdrant / Chroma / LanceDB 存向量
+ BM25: Tantivy / Elasticsearch / Postgres full text
+ reranker: bge-reranker / cross-encoder
+ LLM 生成回答
```

重点是快，不要上来就做复杂图谱。

### 9.2 团队级文献库：几万篇

```text
GROBID 批处理
+ Postgres
+ Qdrant / Weaviate / Milvus
+ Elasticsearch / OpenSearch
+ hybrid retrieval
+ reranker
+ citation graph
+ 定期增量索引
```

这里要做去重、版本管理、DOI/arXiv 合并、metadata 修正。

### 9.3 综述 / 科研助手型系统

```text
结构化 chunk index
+ paper summary index
+ citation graph
+ topic cluster summary
+ GraphRAG / RAPTOR-style summary tree
+ claim-evidence extraction
```

适合问：

```text
领域趋势
路线比较
争议
证据链
关键论文脉络
```

---

## 10. 检索时的 query routing

可以按问题类型路由到不同索引：

| 用户问题 | 推荐路由 |
|---|---|
| “这篇论文讲什么？” | paper-level summary index |
| “某方法的实验设置是什么？” | chunk-level hybrid retrieval + rerank |
| “这几篇论文有什么区别？” | paper-level + method/results section retrieval |
| “这个领域主要路线有哪些？” | cluster summary / GraphRAG / citation graph |
| “哪篇论文最早提出 X？” | BM25 + citation graph + year filter |
| “这个结论有证据吗？” | claim/evidence index + result/discussion section filter |

---

## 11. 最小可行版本配置

从工程上，第一版建议这样做：

```text
1. GROBID 解析 PDF，保留 TEI
2. 按 section + paragraph 切 chunk
3. 每个 chunk 加 title / year / section_path 前缀
4. 建 dense vector index
5. 同时建 BM25 index
6. hybrid top 60
7. rerank top 10
8. 返回 parent section + 邻近 chunk
9. 答案必须引用 paper title、section、page/chunk
10. 做 50–100 个人工测试问题评估 recall 和 citation correctness
```

这个版本已经能覆盖大部分论文问答需求。之后再加 RAPTOR、GraphRAG、citation graph、figure/table parsing，而不是一开始就把系统做复杂。

---

## 12. 最重要的实践建议

### 第一优先级：解析质量 > embedding 模型 > 向量数据库

论文 RAG 的上限通常由 PDF 结构化解析和 chunk 设计决定，而不是由 Milvus、Qdrant、Weaviate、Chroma 之间的差异决定。

### 第二优先级：hybrid + rerank 是默认，不是增强功能

论文里有太多精确术语，只用 dense vector 很容易漏掉关键证据。

### 第三优先级：保留 provenance

每个答案都应该能追到：

```text
paper → section → paragraph/chunk → page → 原 PDF
```

否则它只是“看起来像科研助手”，但不能真正用于文献研究。

---

## 13. 快速 Checklist

### 文档解析

- [ ] PDF 用 GROBID / LaTeX / JATS / HTML 优先解析
- [ ] 保留 TEI / 原始结构化结果
- [ ] DOI、arXiv ID、title、authors、year、venue 规范化
- [ ] 做版本合并和去重

### 分块

- [ ] 按 section + paragraph 分块
- [ ] 每个 chunk 加 paper title、year、section path
- [ ] method、experiment、result、limitation 单独标记
- [ ] 记录 page、paragraph、neighbor chunk、parent section

### 检索

- [ ] dense vector index
- [ ] BM25 / sparse index
- [ ] metadata filter
- [ ] hybrid retrieval
- [ ] reranker
- [ ] parent/neighbor context expansion

### 高级能力

- [ ] figure/table 对象化
- [ ] citation graph
- [ ] paper summary index
- [ ] topic cluster summary
- [ ] GraphRAG / RAPTOR-style summary tree
- [ ] claim-evidence extraction

### 质量评估

- [ ] 设计 50–100 个真实研究问题
- [ ] 评估 recall
- [ ] 评估 citation correctness
- [ ] 评估 answer groundedness
- [ ] 记录失败案例并反向优化 chunk 和 retrieval

---

## 14. 推荐的一句话总结

**针对研究论文的向量知识库，最好的即插即用策略不是“PDF → chunk → embedding”，而是“结构化论文解析 → 论文感知分块 → 多粒度 hybrid retrieval → rerank → 带 provenance 的回答”。**
